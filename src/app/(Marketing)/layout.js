
export const metadata = {
    title: "Trang chủ",
    description: "Smart traffic",
};


const layout = ({ children }) => {
    return (
        <div>{children}</div>
    )
}

export default layout